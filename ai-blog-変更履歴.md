# AI Blog 変更履歴

---

## 2026-06-06

### スマホ一覧フッター下の仕切り線を弱める

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示の記事カードで、フッター下の仕切り線がやや目立って見えるため、一覧の下線を薄く調整。

#### 変更内容

- スマホ表示専用の薄い仕切り線色を追加
- スマホ一覧の記事カード下線を `1px` から `0.5px` に変更
- ライトモード・ダークモードともに半透明の線で控えめに表示

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### ホーム画面アイコンのロゴマークを小さく簡素化

**ファイル**: `output/ai-blog/icons/app-icon.svg`、`output/ai-blog/icons/favicon-16.png`、`output/ai-blog/icons/favicon-32.png`、`output/ai-blog/icons/apple-touch-icon.png`、`output/ai-blog/icons/app-icon-192.png`、`output/ai-blog/icons/app-icon-512.png`

Web Antenna / RSS Reader Cloud と並べた時にAI Blogのロゴマークがやや大きく見えたため、B型の本マークを少し縮小し、細いページ装飾線をなくして簡素化。

#### 変更内容

- アイコン背景色と角丸形状は維持
- B型の本マーク本体を約86%に縮小
- 内側の細いページ線を削除し、綴じ目のみ残したシンプルな構成へ変更
- SVGソースから16px/32px/180px/192px/512pxのPNGアイコンを再生成

#### 確認

- 512px、32px、16pxアイコンを目視確認
- PNGアイコンの画像サイズを確認

---

## 2026-06-04

### LM Studio連携の初回待ちと体感待ち時間を短縮

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.html`

LM Studio連携の初回ロード待ち、読者メモ生成中の無表示時間、短文コメント生成の過剰な待ち時間を抑えるため、高速化処理を追加。

#### 変更内容

- AI補助パネル展開時のプリウォームを、`/models` 取得だけでなく `max_tokens: 1` の極小生成に変更
- 文章作成欄フォーカス時のpingとプリウォームで、同じ軽量生成ヘルパーを使うよう整理
- 読者メモ生成の初回をストリーミング化し、受信途中の有効な行を逐次表示
- 読者メモ再試行と保存後コメントの `max_tokens` を短文用途に合わせて縮小
- Model欄が空の場合、`/v1/models` で解決した先頭モデル名を既存の `aiblog_lmstudio_model` に保存
- 取扱説明書にプリウォーム、読者メモの逐次表示、モデル名自動保存を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### 保存後コメントのLM Studio空応答対策を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.html`

保存後コメント生成で `google/gemma-4-e2b` などが `reasoning_content` だけを返し、`EMPTY_RESPONSE (finish_reason=length)` になる場合に備えて、再試行と補完処理を強化。

#### 変更内容

- 通常の保存後コメント生成に失敗した後、既存の短縮再試行に加えて、さらに短い1文コメント用の最終再試行を追加
- 最終再試行でも空応答・途中終了・不完全コメントになる場合は、本文から短い肯定コメントを補完
- 接続不可やHTTPエラーなど、LM Studio自体の問題は従来どおり診断エラーとして表示
- 取扱説明書に保存後コメントの空応答時の挙動を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-06-02

### カラーパレット全画面表示の下部safe areaを補正

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示でカラーパレットの色を全画面表示した際、iOSのホームインジケータ付近に背景色が残る場合があったため、スポットライト表示中の背景色を画面外周まで選択色に揃えるよう修正。

#### 変更内容

- `#color-spotlight::after` で下側 safe area をスポットライト色で塗る補助レイヤーを追加
- スポットライト表示中は `html` / `body` / `theme-color` を選択中のHEX色へ一時変更
- スポットライトを閉じた時に元の背景色と `theme-color` へ戻す処理を追加

#### 確認

- モバイル幅のブラウザ検証で、スポットライト表示中に `html` / `body` / `theme-color` / 下部補助レイヤーが選択色へ揃い、閉じると元に戻ることを確認

---

## 2026-06-01

### ホーム画面追加時のスマホ縦向き safe area を補正

**ファイル**: `output/ai-blog/ai-blog.html`

ホーム画面に追加したAI Blogを縦向きで開いた時、iPhoneのステータスバーや角丸部分に検索ボタン・設定ボタンが重なる場合があったため、standalone 表示の縦向きだけ上部 safe area を確保。

#### 変更内容

- ホーム画面アプリ表示を `display-mode: standalone` と `navigator.standalone` の両方で判定
- standalone のスマホ縦向きだけ `env(safe-area-inset-top)` を `body` 上部に反映
- 左右の safe area も縦向き standalone 時に反映し、角丸部分でボタンが切れにくいよう調整
- 横向きスマホでは前回どおり上部余白を 0 に戻し、横向きの空白対策を維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### 3アプリのホーム画面アイコン配色を統一

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/manifest.webmanifest`、`output/ai-blog/icons/app-icon.svg`、`output/ai-blog/icons/favicon-16.png`、`output/ai-blog/icons/favicon-32.png`、`output/ai-blog/icons/apple-touch-icon.png`、`output/ai-blog/icons/app-icon-192.png`、`output/ai-blog/icons/app-icon-512.png`

RSS Reader Cloud / AI Blog / Web Antenna のホーム画面アイコンを、RSS Reader Cloud のライトモードタイトル色に合わせて統一。

#### 変更内容

- アイコン背景を `#64779f` の角丸背景へ変更
- アイコン内の使用色を `#64779f` と白のみに整理
- B型の開いた本の形は維持しつつ、他2アプリと余白・サイズ感が揃うよう調整
- SVGソースから180px/192px/512pxのPNGアイコンを再生成
- 同じデザインの16px/32px favicon PNGを追加し、HTMLから参照
- `theme-color` とmanifestのテーマ色を共通ベースカラーへ変更

#### 確認

- 3アプリの512pxアイコンを一覧画像で目視確認
- PNGアイコンの画像サイズを確認

---

### ホーム画面追加時のスマホ横向き上部余白を補正

**ファイル**: `output/ai-blog/ai-blog.html`

ホーム画面に追加したAI Blogを縦向きから横向きへ回転した時、画面上部に空白が残る場合があったため、iOS standalone 表示向けの viewport と高さ計算を調整。

#### 変更内容

- viewport に `viewport-fit=cover` を追加
- iOS ホーム画面アプリ用のステータスバー表示を `black-translucent` に設定
- `visualViewport` の高さを CSS 変数へ反映し、回転後の表示高さを再計算
- 横向きスマホ表示では左右の safe area を考慮しつつ、上方向の不要な余白を抑制
- HTML背景色をテーマに合わせ、万一余白が見えても白く残らないよう調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-31

### ホーム画面アイコンを「B型の開いた本」へ再設計

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/icons/`

AI Blogのアイコンを、表紙にBを載せるデザインから、Bの形そのものが開いた本として機能するデザインへ変更。

#### 変更内容

- `app-icon.svg` からテキスト要素の `B` を削除し、SVGパスだけでB型の本を構成
- Bの左縦線を綴じ目、上下の膨らみをページ束として見えるように調整
- Bの内側に白いページ線を追加し、小さい表示でも本の印象が残るように変更
- faviconをインラインSVGから `icons/app-icon.svg` 参照へ変更し、ホーム画面アイコンとデザインを統一
- `apple-touch-icon.png`、`app-icon-192.png`、`app-icon-512.png` を新デザインで再生成

#### 確認

- 512pxと32px相当でアイコンを目視確認
- PNGアイコンの画像サイズを確認
- `manifest.webmanifest` のJSON構文チェックを実施
- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### スマホ詳細ページのスクロール中の横揺れを抑制

**ファイル**: `output/ai-blog/ai-blog.html`

スマホの記事詳細画面で縦スクロール中に指のズレで画面が左右へ揺れることがあったため、詳細ビューの横方向パンを抑制。

#### 変更内容

- スマホ詳細画面の `#main`、`#detail-panel`、`#detail-view`、`#detail-content` で横方向のオーバースクロールを抑制
- 詳細本文の横はみ出しを閉じ、コードブロックやテーブルは表示幅内に収めるよう調整
- 左端スワイプ戻り中は、横方向の意図が出た時点で早めに横移動をロック
- 縦スクロール主体の操作は従来どおりスクロールできるよう維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- スマホ相当の詳細画面で縦スクロールと左端スワイプ戻りを確認

---

### スマホ詳細ページの左端スワイプ戻りを安定化

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ実機で記事詳細ページの左端スワイプ戻りが成功しない場合があったため、ジェスチャー開始位置と判定方法を調整。

#### 変更内容

- 詳細画面の左端に透明なスワイプ判定エリアを追加
- 左端判定幅と戻り判定距離を調整し、端ぴったりでなくても戻りやすく変更
- 横方向の戻る操作は拾いつつ、縦スクロールは維持するよう `touch-action` と判定条件を調整
- 詳細画面の横方向オーバースクロールを抑制

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- スマホ相当の縦向き・横向き表示で左端スワイプ戻りを確認

---

### スマホ横向き表示と左端スワイプ戻りを調整

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ横向き表示でもモバイル向けの画面構成を使い、記事表示領域が狭くなりすぎないよう調整。記事詳細ページの左端スワイプ戻りも、実機のタッチ操作で反応しやすいよう改善。

#### 変更内容

- モバイル判定を幅720px以下だけでなく、横向きスマホ相当の低い画面高にも適用
- 横向きスマホ時のヘッダー、記事一覧ヘッダー、記事カード、詳細ヘッダーを圧縮
- 左端スワイプ戻りの開始判定幅を広げ、Pointer Events に加えて Touch Events でも戻れるよう対応
- 横向きスマホでも検索ショートカットがモバイル検索表示を開くよう統一

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- 横向きスマホ相当の表示でヘッダーと記事一覧の高さ、詳細ページの左端スワイプ戻りを確認

---

## 2026-05-30

### スマホ詳細ページに左端スワイプ戻りを追加

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示の記事詳細ページで、RSS Reader Cloud と同様に画面左端から右へスワイプすると一覧へ戻れるようにした。

#### 変更内容

- スマホ詳細ページの左端24pxから右方向へスワイプした場合に一覧へ戻る処理を追加
- 縦スクロールやボタン、リンク、入力欄の操作には干渉しないよう判定を追加
- スマホ表示では `← Articles` ボタンを非表示化
- 戻るボタン分の余白を削り、詳細タイトルと本文を上へ移動

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### 設定モーダルに表示テーマ切り替えを追加

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示でも設定画面からライトモード・ダークモードを切り替えられるようにした。

#### 変更内容

- 設定モーダルの先頭に「表示設定」セクションを追加
- RSS Reader Cloud と同様に「表示テーマ」行と `ライト` / `ダーク` ボタンを追加
- 設定内ボタンと既存のテーマボタンが同じテーマ切り替え処理を使うよう整理
- テーマ切り替え後、設定内ボタンの表示名とアクセシブル名を即時更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### ホーム画面アイコンを本の表紙デザインへ再調整

**ファイル**: `output/ai-blog/icons/`

スマホのホーム画面で表示されるAI Blogアイコンが、faviconと同じく本に見えるようデザインを再調整。

#### 変更内容

- ピンク背景と白い `B` は維持
- 白い表紙外枠、背表紙ライン、ページ線を追加
- `app-icon.svg` を元データとして更新
- `apple-touch-icon.png`、`app-icon-192.png`、`app-icon-512.png` を新デザインで再生成

#### 確認

- PNGアイコンの画像サイズと表示内容を確認

---

### ホーム画面追加用アイコンを追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/manifest.webmanifest`、`output/ai-blog/icons/`

スマホでAI Blogをホーム画面へ追加したとき、現在のfaviconと同じデザインのアイコンで表示されるようにした。

#### 変更内容

- 既存faviconのピンク背景、白い `B`、白い上線をもとにホーム画面用アイコンを作成
- `apple-touch-icon.png`、`app-icon-192.png`、`app-icon-512.png`、元データ `app-icon.svg` を追加
- Web App Manifest `manifest.webmanifest` を追加
- HTMLに `apple-touch-icon`、`manifest`、スマホWeb App用メタタグを追加

#### 確認

- PNGアイコンの画像サイズと表示内容を確認
- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- `manifest.webmanifest` のJSON構文チェックを実施

---

### スマホ上部ヘッダーとArticles欄の高さを調整

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示で記事一覧の表示量を増やすため、タイトル周辺とArticles欄の縦余白を再調整。

#### 変更内容

- スマホ表示のヘッダーパディング、タイトルの行高、タイトル余白を縮小
- スマホ表示の検索ボタンと設定ボタンの高さを少し圧縮
- Articles欄の上下余白とタグ選択欄の高さを、見た目のバランスを保つ範囲で縮小

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### スマホ詳細・編集画面の左右余白を縮小

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示で本文と編集画面をより画面端まで使えるように調整。

#### 変更内容

- スマホ詳細表示の本文コンテナ幅を広げ、左右パディングを縮小
- スマホ詳細表示のヘッダーと戻るボタン位置を本文幅に合わせて調整
- スマホ編集画面のコンテナ幅、ヘッダー、メタ情報、本文入力欄の左右余白を縮小

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### スマホ一覧の検索欄を折りたたみ化

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示で記事一覧の表示領域を広げるため、検索欄とArticlesヘッダーの高さを調整。

#### 変更内容

- スマホ表示では検索欄を初期非表示にし、ヘッダーの検索ボタンから開くよう変更
- 検索ボタン押下時と `/` ショートカット時に検索欄を表示してフォーカス
- 検索欄でEscを押すとスマホ検索欄を閉じる動作を追加
- スマホ表示のArticles欄のパディング、文字サイズ、タグ選択欄の高さを圧縮

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### スマホ表示の設定ボタンと設定モーダルを整理

**ファイル**: `output/ai-blog/ai-blog.html`

スマホ表示でCloud設定へ到達できるようにし、設定モーダル内のLM Studio設定とCloud設定の配置を分離。

#### 変更内容

- スマホ表示では `import` / `export` ボタンを非表示にし、設定ボタンを表示
- 設定ボタンのツールチップとアクセシブル名を「設定」に変更
- 設定モーダルの見出しを「設定」に変更し、下に「LM Studio設定」と「Cloud設定」を分けて表示
- `接続テスト` をLM Studio設定内へ、`同期` と `保存` をCloud設定内へ移動
- スマホ表示の設定モーダルではLM Studio設定を非表示にし、Cloud設定だけ表示

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### 取扱説明書をHTML版として再構成

**ファイル**: `output/ai-blog/ai-blog-取扱説明書.html`、`output/ai-blog/ai-blog-取扱説明書.md`

AI Blog の取扱説明書を、人間が読みやすく、AIにも仕様を抽出しやすいHTML文書として作り直した。

#### 変更内容

- HTML版の取扱説明書を新規作成
- 概要、起動と初期設定、画面構成、記事操作、Markdown、タグ検索、パレット、AI補助、LM Studio、Cloud同期、スマホ表示、import/export、データ仕様、トラブル対応を章立てで整理
- `application/ld+json` とAI向け構造化サマリーを追加
- 旧Markdown説明書はHTML版への案内に差し替え

#### 確認

- HTML文書の目次リンク、必須セクション、JSON-LD、AI向け要約の静的検査を実施

---

## 2026-05-28

### Supabase Cloud同期とスマホ表示を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/supabase-schema.sql`、`output/ai-blog/ai-blog-取扱説明書.md`

AI Blogを既存ファイルのままCloud対応し、スマホでは一覧と詳細を横スライド式で使える表示へ変更。

#### 変更内容

- 設定モーダルにSupabase URL、anon key、共有ID、同期ボタンを追加
- 記事と基本設定をSupabase REST APIで同期する処理を追加
- 初回同期はローカル記事とクラウド記事をIDでマージし、同一IDは更新日時が新しい方を採用
- Cloud未設定時は従来どおり`localStorage`だけで動作
- LM Studio設定、下書き、検索語、生成キャッシュ、診断表示は同期対象外として維持
- AI Blog用のSupabaseスキーマを追加
- スマホ表示で記事一覧/詳細を横スライド化し、画像あり記事は右サムネイル型リストで表示
- スマホではLM Studio設定ボタンとAI補助パネルを非表示化

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカルHTTPサーバーでPC幅/スマホ幅の表示を確認

---

## 2026-05-25

### ボタンツールチップの表示位置を調整

**ファイル**: `output/ai-blog/ai-blog.html`

ボタンにカーソルを重ねた時の独自ツールチップがカーソルに近く、文字が見づらい場合があったため、下向き表示の説明ラベルを少し下に離して表示するよう調整。

#### 変更内容

- `button[data-tooltip]::after` の下方向オフセットを拡大
- `button[data-tooltip]::before` の矢印位置も合わせて下へ調整
- 上方向表示を使うテーマボタンのツールチップ位置は維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカルブラウザで Palette などのボタン説明がカーソルから離れて表示されることを確認

---

## 2026-05-24

### 取扱説明書を単体で読める構成へ整理

**ファイル**: `output/ai-blog/ai-blog-取扱説明書.md`

AI Blog の取扱説明書が別ツールの説明を前提にした記述になっていたため、AI Blog 単体の説明として読めるように全面整理。

#### 変更内容

- 概要、起動方法、画面構成、記事操作、Markdown編集、タグ検索、パレット、AI補助、LM Studio設定、import / export を独立した説明として再構成
- 「別ツールとの差分」や「基本操作は同じ」といった前提依存の記述を削除
- 既存データとの互換は、保存キーとJSON形式の説明として独立セクションに整理
- キーボードショートカット、下書き、保存後コメント、診断コードの説明を追加・整理

#### 確認

- Markdown 文書のみの変更のため、構文チェック対象なし

---

## 2026-05-23

### 保存後コメントの31B級モデル向け軽量化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

`google/gemma-4-31b` の保存後コメント生成で `reasoning_content` だけに出力上限を使い、本文応答が空になる問題を抑制。

#### 変更内容

- 30B級以上かつ思考抑制対象のモデルでは、保存後コメントを短い軽量プロンプトで生成
- 保存後コメントの軽量生成に短めの専用タイムアウトを設定し、長い失敗待ちを削減
- 軽量生成でも空応答やタイムアウトになる場合は、本文の具体語を使った画面用の補助コメントを表示
- 補助コメントは従来どおり、**コメントを保存** を押した場合だけ本文へ追記
- 取扱説明書に30B級以上の保存後コメント軽量化と補助コメント表示を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

### 30B級モデルの文章作成を軽量生成へ直行

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

`google/gemma-4-31b` の文章作成で標準生成と再試行の失敗待ちが長く、最終再試行でようやく成功する状態を改善。

#### 変更内容

- 30B級以上かつ思考抑制対象のモデルでは、文章作成を最初から軽量生成ルートへ直行
- 標準生成と短縮再試行の失敗待ちを省き、LM Studioへの生成リクエスト回数を削減
- 軽量生成ルートでは送信材料と既存本文コンテキストを短めに圧縮
- 成功診断の状態表示に `軽量生成` を追加
- 取扱説明書に30B級以上の文章作成軽量生成ルートを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-22

### LM Studio連携の診断表示と進捗更新を軽量化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio連携の生成品質と文字数目安を維持しながら、成功・エラー・進捗表示を軽くし、体感速度とUI応答性を改善。

#### 変更内容

- 診断欄を短い要約と折りたたみ式の **診断詳細** に分離
- 成功時は短い成功要約だけを表示し、詳細診断は閉じた状態で確認できるよう変更
- エラー時は診断コードと短い対処を先に表示し、model、max_tokens、経過時間、応答抜粋などは詳細側へ移動
- 文章作成の進捗DOM更新を0.5秒間隔から1秒間隔ベースへ抑制
- 文章作成のストリーミング本文表示は維持
- 文章作成が再試行後も `EMPTY_RESPONSE (finish_reason=length)` になる場合、さらに短い材料で最終再試行するフォールバックを追加
- 論考形式などで短すぎる完全文や末尾整形結果を成功扱いしないよう、文章形式と再試行段階に応じた最低文字数判定を追加
- 再試行後も文章が短すぎる場合は、最終再試行フォールバックへ進むよう変更
- 30B級以上の大きい出力上限では、非ストリーミング生成の全体タイムアウトも最大5分に延長
- 接続テストとLM Studio生成失敗トーストを短い通知へ変更
- 保存後コメントは保存完了を先に通知し、LM Studioコメント生成中・失敗をコメントカード内の軽い状態表示へ変更
- 詳細診断は画面上のその場表示だけにし、保存データやexportには含めない

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-22

### AI補助タブの生成ボタン表示を統一

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常編集画面のAI補助で、文章作成と読者メモのボタン配置・生成中カーソル表示が揃っていなかったため調整。

#### 変更内容

- 読者メモの **生成** ボタンをAI補助ヘッダー右側から読者メモ欄内の右寄せアクション行へ移動
- 読者メモと文章作成の生成ボタンに共通のボタンスタイルを適用
- 生成中の生成ボタンは、読者メモ・文章作成とも `cursor: wait` になるよう統一
- **本文末尾へ追加** ボタンは、生成文章がない無効状態を示すため `cursor: not-allowed` を維持
- 取扱説明書の読者メモ・文章作成ボタン位置説明を更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-21

### 文章作成ストリーミングの30B級モデル待機時間を延長

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

`google/gemma-4-31b` の文章作成で、全体タイムアウト前に30秒のストリーミング無通信タイムアウトへ到達する問題を抑制。

#### 変更内容

- 30B級以上のモデル判定を共通ヘルパー化
- 文章作成ストリーミングの無通信タイムアウトを、30B級以上では最大90秒に延長
- 30B級以上の文章作成ストリーミング全体タイムアウトを最大5分に延長
- 初回本文チャンクを受け取る前は無通信タイムアウトを開始せず、モデル計算待ちは全体タイムアウトで扱うよう変更
- `google/gemma-4-31b` でも `/no_think` を付与し、内部推論だけで本文受信が遅れるケースを抑制
- 通常サイズのモデルは従来どおり12〜30秒の無通信タイムアウトを維持
- `TIMEOUT` 診断に `timeout` 種別と `stream_idle_timeout` 秒数を追加
- 取扱説明書に、30B級以上の文章作成ではストリーミング無通信待機を長めにすることを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-20

### 読者メモ生成の空応答リトライを強化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio の `google/gemma-4-e2b` で読者メモ生成時に `reasoning_content` だけで出力上限に達し、`EMPTY_RESPONSE (finish_reason=length)` になる問題を抑制。

#### 変更内容

- `google/gemma-4-e2b` を思考出力抑制対象として扱うよう変更
- 読者メモ生成で初回から `forceNoThink` を有効化
- 読者メモの初回 `max_tokens` を 170 から 360 に拡大
- 空応答、タイムアウト、読者コメントなし、`finish_reason=length` の場合に短いプロンプトで再試行
- 再試行時は `max_tokens` を 700 に増やし、最終出力だけを返す指示を強化
- 取扱説明書の推論抑制対象に `google/gemma-4-e2b` を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-20

### 画像なしタイルの本文開始位置を整列

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常表示の画像なしタイルで、タイトル行数によって本文抜粋の開始位置がずれないよう調整。

#### 変更内容

- 画像なしタイルのタイトル領域を3行分の固定高さに設定
- 通常タイトル側の強いセレクタに負けないよう、画像なしタイトルの固定高さルールを通常タイトル定義の後ろで適用
- 1行・2行タイトルでも、3行タイトルと同じ高さを確保
- 3行を超えるタイトルは従来どおり省略表示し、4行目が途中表示されないよう最大高さも固定
- 本文抜粋の行数は、画像あり最大3行・画像なし最大6行のまま維持
- 取扱説明書に画像なしタイルのタイトル領域ルールを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカル表示で画像なしタイトルに3行分の `height` / `min-height` / `max-height` ルールが読み込まれることを確認
- 対象タイトル「ASICS Runwalk & CORTHAY ... ASICS WALKING JOURNAL」で、画像なしタイトル用の21px固定高さルールが算出されることを確認
- 静的検査で画像なし本文抜粋の最大6行表示と通常タイトルの最大3行省略が維持されていることを確認

---

## 2026-05-20

### 通常タイル一覧の余白と横並び数を調整

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常表示の記事タイルをコンパクトにし、同じ画面幅でより多くの記事が横並びになるよう調整。

#### 変更内容

- 通常タイル一覧のカード幅を 350px 固定から 240〜280px の可変幅へ変更
- 記事間の縦横gapと左右paddingを縮小し、余白の密度を調整
- 画像の固定高さをやめ、カード幅に合わせた16:9比率で自然に縮むよう変更
- タイトル、本文抜粋、カード内余白を少し締め、一覧性を向上
- モバイル幅では中央寄せの単列表示を維持
- 取扱説明書のタイル幅説明を更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### ヘッダーボタンの並び順を変更

**ファイル**: `output/ai-blog/ai-blog.html`

ヘッダー右側の操作ボタンを、import、export、LM Studio設定の順に変更。

#### 変更内容

- LM Studio設定ボタンを import / export の後ろへ移動
- import、export、設定モーダルのクリック処理は変更なし

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### 通常タイルの情報表示順を変更

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常表示の記事タイルで、情報が日付、タイトル、画像、本文、カラーバーの流れで読めるよう表示順を変更。

#### 変更内容

- 通常タイルの画像をタイトルの下へ移動
- 日付・タグ行、タイトル、画像、本文抜粋、カラーストリップの順で表示されるようHTML生成順を調整
- 取扱説明書のタイル構成説明を更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### 編集ボタンを非アクティブ表示で常時表示

**ファイル**: `output/ai-blog/ai-blog.html`

記事詳細表示の編集ボタンを、通常時も控えめに見える状態へ変更。

#### 変更内容

- `#edit-btn` を完全非表示にせず、低透明度の非アクティブ表示で常時表示
- 親要素hoverで表示する既存ルールをやめ、ボタン自身のhover/focusでアクティブ表示に変更
- hover/focus時はアクセント色と背景を戻し、編集操作が可能な状態として分かるよう調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### 未ピン留めボタンの表示を控えめに調整

**ファイル**: `output/ai-blog/ai-blog.html`

記事詳細表示と旧タイルオーバーレイのピンボタンについて、未ピン留め状態が鮮やかに見えすぎないよう調整。

#### 変更内容

- `#pin-btn` の非アクティブ状態に低彩度・低透明度の専用表示を追加
- 未ピン留め状態では背景を透明にし、枠線とアイコン色を muted 系に変更
- hover時は少し色と透明度を戻し、操作可能であることが分かる表示に調整
- `#pin-btn.pinned` は従来どおりライトモードで赤系、ダークモードで黄色系の強調表示を維持
- `#tile-overlay-pin-btn` にも同様の非アクティブ表示を適用

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### URL・Tags未設定表示の斜体を解除

**ファイル**: `output/ai-blog/ai-blog.html`

編集画面のURL・Tags欄が未設定のときに表示される `未設定` バッジを通常書体に変更。

#### 変更内容

- `#edit-meta-summary.empty` の斜体指定を削除

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### MBTI保存後コメントプロンプトを文書化

**ファイル**: `output/ai-blog/ai-blog-MBTI保存後コメントプロンプト.md`

記事保存時にLM Studioへ送信する保存後コメント用プロンプトを、タイプ別に確認できるよう文書化。

#### 変更内容

- 通常生成とリトライ生成の共通プロンプト形式を記載
- 16タイプごとの `focus` と `style` を表形式で整理
- 保存後コメント生成時の制約と適用範囲を補足

---

## 2026-05-19

### 保存後コメントにMBTI設定を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

記事保存後に表示するLM Studioコメントを、設定でON/OFFおよびMBTIタイプ別に調整できるよう変更。

#### 変更内容

- LM Studio設定モーダルに **記事保存後にコメントを生成する** チェックボックスを追加
- MBTI公式Type Table配列の4×4タイプテーブルを追加し、保存後コメント向けタイプを1つ選べるよう変更
- 既定値は保存後コメントON、MBTIタイプは `INFJ` に設定
- 保存後コメント生成プロンプトを選択MBTIタイプ別に切り替えるよう変更
- タイプごとに肯定する観点と文体・語調を分け、コメントの出力差が出やすいよう調整
- 保存後コメント生成キャッシュにMBTIタイプを含め、タイプ変更後は別コメントを生成するよう変更
- 取扱説明書に保存後コメント設定とMBTIタイプテーブルの説明を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- `INFJ`、`ENTJ`、`ESFP`、`INTP` のプロンプトを静的に確認し、タイプ別の語調指示が異なることを確認

---

## 2026-05-19

### パレットモードボタンの位置を固定

**ファイル**: `output/ai-blog/ai-blog.html`

パレットモード切替後もPaletteボタンの位置が動かないよう、記事一覧ヘッダーのボタン順を調整。

#### 変更内容

- パレットモード時の表示順を、暗め、基準、明るめ、Palette の順に変更
- Paletteボタンをボタン群の右端に置き、切替前後で位置が変わりにくい構成に変更
- パレットモード時の新規作成ボタンは位置保持用に領域だけ残し、表示は隠すよう変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### LM Studio設定をヘッダーへ移動

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio連携の設定導線を編集画面内からヘッダーへ移動。

#### 変更内容

- import / export ボタン列に歯車アイコンの LM Studio設定ボタンを追加
- 既存のLM Studio設定モーダルをヘッダーの歯車ボタンから開くよう変更
- 編集画面内のAI補助パネルから設定ボタンを削除
- `LM Studio を使用する` がオンの場合だけ、編集画面のAI補助パネルを表示するよう変更
- LM Studio設定保存後、AI補助パネルの表示/非表示を即時反映するよう変更
- 取扱説明書のLM Studio設定手順をヘッダー歯車ボタン経由に更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### exportをJSONのみに変更

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

export操作をJSON出力のみに整理。

#### 変更内容

- exportボタンをドロップダウンから `export` の単独ボタンに変更
- Markdown exportボタンとMarkdown出力処理を削除
- 未使用になったexportメニューCSSとMarkdown export用ヘルパーを削除
- 取扱説明書の基本操作をJSON export表記へ更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-19

### 通常タイルのタイトルと本文抜粋を調整

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常表示の記事タイルで、未入力タイトルと本文抜粋量の表示を調整。

#### 変更内容

- タイトル未入力時の表示を `(タイトルなし)` から `Untitled` に変更
- タイル上のタイトルを最大3行で省略するよう変更
- 画像なしタイルの本文抜粋を、画像ありタイルの2倍となる最大6行・300文字に変更
- 画像ありタイルの本文抜粋は従来どおり最大3行・150文字を維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-18

### 通常表示カラーストリップの調整

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常表示の記事タイル最下部に表示される Palette のカラーストリップを見やすく調整。

#### 変更内容

- 通常表示の記事タイルに出るカラーストリップの高さを 10px から 20px に変更
- カラーストリップ上下の枠線を削除
- 取扱説明書のカラーストリップ説明から「細い」表現を削除

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカルHTTPサーバー経由で読み込んだCSSOMにて、`.blog-card-palette` が `height: 20px` かつ上下枠線なしであることを確認

---

## 2026-05-18

### コードレビュー指摘の安全対応

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

`reference/ai-blog-コードレビュー-2026-05-18.md` を確認し、機能低下や仕様変更のデメリットが小さい項目を反映。

#### 変更内容

- localStorage 読み込み、別タブ同期、JSON import 時に記事データを正規化する処理を追加
- import 時のURL・タグ・Palette・ピン状態の扱いを堅牢化
- 記事ごとに自動下書きを保持し、別記事の下書きで上書きされにくい形式へ変更
- 保存失敗時に、記事追加・編集・削除・ピン留め・AIコメント追記が成功扱いで進まないよう調整
- パレットギャラリーカード選択時に `selectedId` も更新するよう修正
- 記事タイルとパレットギャラリーカードを Enter / Space で選択できるようキーボード操作を追加
- 詳細表示のタグをクリック可能な `button` として出力するよう変更
- AI補助タブに `role="tab"` / `role="tabpanel"` と選択状態を追加
- 検索入力の再描画を短くデバウンスし、入力中の不要な再描画を抑制
- LM Studio のストリーミング生成にアイドルタイムアウトを追加
- LM Studio の読者メモ、文章作成、保存後コメントで、古い非同期結果が後から画面へ反映されないよう調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-18

### 通常表示を統一タイルと全画面詳細へ変更

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常表示の記事一覧を、記事数や選択状態で表示内容が変わらない統一タイル表示へ調整。

#### 変更内容

- 通常表示の記事タイルを 350px 前後の折り返しグリッドに変更
- 各タイルで画像、日付、タグ、タイトル、本文抜粋を同じ構成で表示
- タイルの角丸、枠線、カード背景を外し、ホバー時だけ境界がわかる表示へ変更
- 横に並ぶ記事数が増えるよう余白とタイル幅を調整
- 画像がない記事では画像欄を表示せず、テキスト主体のシンプルなタイルとして表示
- 本文ありを示すペンマークと、本文なし記事の案内文を通常タイルから削除
- 通常タイル右下の `読む ↗` リンク表示を削除
- Palette のカラーストリップをタイル最下部へ移動
- ピン留めアイコンをライト/ダーク共通で `📍` に統一
- 通常表示のタグサイドバーを廃止し、Articles見出し横の単一選択ドロップダウンでタグ絞り込みを行うよう変更
- パレットギャラリーモードでは旧タグサイドバーとタグドロップダウンを隠し、左側・上側も選択した背景色で統一
- ダークモード時のヘッダータイトル hover エフェクトを無効化
- 編集画面の Split ボタンでプレビュー領域が表示されないCSS崩れを修正
- 詳細・編集本文のスクロールバー、Markdownリファレンス、Palette入力欄など旧デザインが残っていた部品をブログ調デザインへ統一
- 詳細表示のヘッダー固定をやめ、タイトル・日付・更新日・本文が同じ読書ページ内でスクロールするよう変更
- 保存後に表示されるAIコメントへ閉じる `×` ボタンを追加
- 本文が空、または画像・Paletteカラーのみの記事では、保存後コメント・読者メモ・文章生成などのLM Studio連携を実行しないよう変更
- 通常記事一覧を固定350pxトラックのグリッドに変更し、ブラウザ幅によるタイル幅の揺れや列数混在を抑制
- Chromeでタイルのpaddingが350pxトラック外へはみ出して重なって見える問題を修正
- 通常タイルの上下間隔を明示し、ホバー時の影を廃止して上下の記事にかからないアウトライン表示へ変更
- 詳細表示の操作ボタンを編集・ピンの順に変更し、編集ボタンは操作エリアhover/focus時だけ表示するよう変更
- 詳細表示のタイトル・日付エリアと本文の間に出ていた区切り線を削除
- パレットギャラリーボタンを絵文字からCSS描画の6分割カラーホイールアイコンへ変更し、新規作成ボタンと同程度の円形サイズに統一
- 編集画面のURLカラー抽出ボタンも同じ6分割カラーホイールアイコンを使うシンプルなピル型ボタンへ変更
- 主要ボタンのブラウザ標準ツールチップを、短く素早く表示されるCSSベースの独自ツールチップへ変更
- 編集本文の1行目がSplit/Markdownツールバーに重ならないよう、本文エリア上部の余白を調整
- 詳細表示の日付・タグ行から本文までの余白を約2/3に縮小
- 記事選択時は一覧を隠し、詳細表示を全画面の読書ページとして表示
- 詳細表示に一覧へ戻る `← Articles` ボタンを追加
- `⊞` ボタンを廃止し、ヘッダー中央の `AI Blog` タイトルクリックで一覧へ戻るよう変更
- ヘッダー中央のデフォルト表示タイトルを `My Blog*` に変更
- 詳細表示の本文カラムを中央揃えにし、編集画面でも記事一覧カラムを隠して中央の編集画面として表示

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ブラウザで通常表示、タグドロップダウン、全画面詳細、一覧復帰、ライト/ダークモード、パレットギャラリー表示を確認

---

## 2026-05-17

### チャットモード削除とタイルボタンの全体表示化

**ファイル**: `output/ai-blog/ai-blog.html`

チャットモードのみ削除し、タイルボタンは旧タイル画面ではなく初期状態のブログカード一覧へ戻る操作に変更。

#### 変更内容

- ヘッダーのチャットボタン、チャット入力欄、チャットモード用CSS、チャット投稿イベントを削除
- `viewMode` は通常表示とパレットギャラリーのみを扱うよう整理
- `⊞` ボタンと `t` ショートカットは、選択中の記事を解除してArticlesのブログカード一覧へ戻る動作に変更
- 旧タイルモード画面へ切り替える分岐を削除し、記事選択時は通常の詳細表示を開くよう統一

#### 確認

- `node` による埋め込みJavaScript構文チェックを実施済み

---

### 通常Articlesカードにカラー表示を追加

**ファイル**: `output/ai-blog/ai-blog.html`

既存のPalette表示部品を参照し、通常表示のブログカードにも記事のPaletteを控えめに表示。

#### 変更内容

- 通常表示のブログカードで、`entry.palette` に有効なHEXカラーがある場合のみサムネイル下に細いカラーストリップを表示
- 既存の `buildPaletteBlocksHtml()` を再利用し、保存形式やPalette編集機能は変更なし
- 詳細・編集表示で左側のカードがコンパクトになる状態でも、カラー帯は8pxの細い表示に調整

#### 確認

- `node` による埋め込みJavaScript構文チェックを実施予定
- ブラウザで通常表示・詳細表示中のArticlesカードを確認予定

---

### 通常表示をブログ風デザインへ刷新

**ファイル**: `output/ai-blog/ai-blog.html`

AI Blogの機能・保存形式・LM Studio連携を維持したまま、初期表示を一般的なブログに近いエディトリアル調へ変更。

#### 変更内容

- 通常リスト表示を、縦長サムネイル・タイトル・日付・タグ・本文抜粋を持つブログカード型グリッドへ変更
- 本文中の最初の画像をカードサムネイルとして表示し、画像がない記事は番号入りプレースホルダーを表示
- 空状態では詳細パネルを隠し、記事一覧を広く表示
- 記事詳細のタイトル・本文幅・余白・メタ情報を読み物ページ風に調整
- ヘッダー、タグサイドバー、ボタン、編集画面、AI補助パネルの配色をミニマルなブログ調に統一
- 既存のimport/export、保存形式、ショートカット、AI補助、パレット機能は維持

#### 確認

- `node` による埋め込みJavaScript構文チェックを実施予定
- ブラウザで通常表示・記事詳細・編集画面・各表示を確認予定

---

## 2026-05-16

### LM Studio文章作成の体感速度を改善

**ファイル**: `output/ai-blog/ai-blog.html`

文章作成タブの待ち時間を短く感じられるよう、LM Studio連携の生成フローを改善。

#### 変更内容

- 文章作成のみストリーミング生成に対応し、本文案を受信しながら逐次表示
- 生成中の診断欄を1行のコンパクト表示にし、経過秒・受信文字数・状態を表示
- 生成ボタンにも受信文字数を表示
- AI補助展開時のモデル解決先取りと、材料欄初回フォーカス時の軽量pingを追加
- 材料欄に1500字の入力上限と文字数カウンタを追加し、800字超で注意表示
- 途中失敗時は表示済み本文を残し、成功時だけ生成キャッシュへ保存するよう調整
- 文章作成プロンプトの重複表現を圧縮し、文体・長さの品質制約は維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- LM Studio実機での日記形式・論考形式のストリーミング表示、再試行、材料上限確認は未実施

---

## 2026-05-15

### コードレビュー指摘の安全対応

**ファイル**: `output/ai-blog/ai-blog.html`

`reference/ai-blog-コードレビュー-2026-05-15.md` の指摘から、副作用が小さく即時対応できる項目を修正。

#### 変更内容

- 保存後コメントの再試行プロンプトを、初回より短い実体のある最小プロンプトへ変更
- 通常編集・タイル編集の保存対象が見つからない場合に、エラートーストを表示してサイレント失敗しないよう変更
- 通常編集のキャンセル時に `selectedId` がない場合は空状態へ戻すよう変更
- AI提案追加処理の常に真になる二重 `if` を削除
- 未使用の `preview-only` 分岐と空の `scheduleAiAssist()` 呼び出しを削除
- import時に既存記事の `pinned` をローカル値として保持
- LM Studio生成キャッシュを最大16件に制限

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-15

### 保存後コメントの長さを調整

**ファイル**: `output/ai-blog/ai-blog.html`

保存後コメントが短すぎて意味が判然としないケースに対応。

#### 変更内容

- 保存後コメントのプロンプトを、1文から2文・80〜140字程度へ変更
- INFJ向けの穏やかな肯定方針と、本文の具体語を含める制約は維持
- 保存後コメントの `max_tokens` を少し増やし、途中切れしにくく調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-15

### 保存後コメントをINFJ向けに調整

**ファイル**: `output/ai-blog/ai-blog.html`

保存後に表示される肯定コメントの方向性を、INFJの人が静かに嬉しく感じやすい文へ調整。

#### 変更内容

- 保存後コメントのプロンプトを、内面の深さ・誰かを思う視点・言葉にしにくい感覚を穏やかに認める方向へ変更
- 既存の「本文の具体語を含める」「大げさな称賛や助言を避ける」「保存通知だけにしない」「句点で終える」制約は維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-15

### 保存後コメントの途中切れ対策

**ファイル**: `output/ai-blog/ai-blog.html`

保存後コメントが途中の語句で切れたまま表示されるケースに対応。

#### 変更内容

- 保存後コメントも句点などで完結しているかを判定
- 初回生成が未完結の場合は、最小プロンプトで再試行
- 再試行後も未完結の場合はポジティブコメントとして採用しない
- 保存後コメントのプロンプトに、句点で終える指示を追加

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-15

### 保存後コメントで no_think を強制

**ファイル**: `output/ai-blog/ai-blog.html`

保存後コメント生成で再度 `finish_reason=length` になるケースに対応。

#### 変更内容

- 保存後コメント生成だけ、モデル名に関係なく `/no_think` 付きの単一ユーザーメッセージ形式を強制
- 保存後コメントの送信本文量をさらに圧縮
- 初回・再試行とも、本文応答にトークンが回るよう `max_tokens` を調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-15

### 保存後コメントの空応答対策

**ファイル**: `output/ai-blog/ai-blog.html`

保存後コメント生成で `finish_reason=length` により空応答になるケースに対応。

#### 変更内容

- 保存後コメントの初回プロンプトを1文生成に絞り、送信本文量をさらに圧縮
- 初回が `EMPTY_RESPONSE`、`TIMEOUT`、`finish_reason=length` の場合だけ、さらに短い最小プロンプトで一度再試行
- 再試行時のみ `max_tokens` を増やし、通常時の速度低下を抑えるよう調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-15

### 保存後コメントの汎用文対策

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studioの保存後コメントが「保存しました。」だけになるケースに対応。

#### 変更内容

- 保存後コメントのプロンプトに、「保存しました」「保存完了」のような保存通知だけの文を禁止する指示を追加
- LM Studioが保存通知だけを返した場合は、ポジティブコメントとして採用しないよう判定を追加
- 保存後コメント生成に失敗した場合、最後に通常の成功トーストで上書きせず、コメントカードに失敗理由を残すよう変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-15

### LM Studio生成を高速化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio連携の生成時間を短くするため、品質条件を残しながら入力・出力・再試行を軽量化。

#### 変更内容

- 読者メモと保存後コメントをJSON生成から短いプレーンテキスト生成へ変更
- 文章作成の送信内容を `JSON.stringify` から短いラベル付きテキストへ変更
- 文章作成の初回 `max_tokens` を少し抑え、未完結時の再試行だけ大きめの上限を使うよう調整
- 同じ入力の読者メモ・保存後コメント・文章作成は、ページ表示中の一時キャッシュから即時表示するよう変更
- Model欄が空欄の場合の `/v1/models` 取得結果をページ表示中だけ再利用し、設定保存時にキャッシュをクリアするよう変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 論考形式の文体を調整

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

論考形式の生成が短く硬い文章になりやすい問題に対応。

#### 変更内容

- 論考形式の指示を、硬い論文調ではなくブログの論考随筆として書く内容に変更
- 具体的な場面や比喩から、人間観・組織・経営への考察へつなげる指示を追加
- 「である」調の連続や専門語だけの文章を避け、読者に考えを手渡す文体へ調整
- 論考形式のみ初回 `max_tokens` を 1300、再試行を 1700 に変更
- 取扱説明書に、論考形式の目安文字数と文体方針を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 文章作成の途中切れ対策を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio文章生成で本文が途中の語句で切れても成功扱いになるケースに対応。

#### 変更内容

- 文章作成の生成結果が句点などで完結していない場合は `INCOMPLETE_DRAFT` として再試行対象に変更
- 十分な長さの完全文がある場合は、不完全な末尾だけを外して表示
- 初回 `max_tokens` を 1050、再試行を 1500 に調整し、速度を大きく戻さず完結しやすくした
- 文章作成の目安文字数を 500〜900字に調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 文章作成の生成時間を短縮

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

Gemma 4 系で文章作成は成功するものの時間がかかるため、品質条件を残しながら初回生成を軽量化。

#### 変更内容

- 文章作成の初回生成をJSON形式から本文のみ生成へ変更
- 既存本文コンテキストと材料の送信量を少し圧縮
- 初回 `max_tokens` を 850 に下げ、空応答時の再試行も 1200 に抑制
- 文章作成の目安文字数を 400〜800字に調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 文章作成の空応答時に本文のみ再試行を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

Gemma 4 系でも文章作成時に `reasoning_content` だけで出力上限を使い切り、`content` が空になるケースに対応。

#### 変更内容

- 文章作成のJSON生成が `EMPTY_RESPONSE`、`JSON_PARSE`、`EMPTY_DRAFT` になった場合、本文だけを返す短いプロンプトで再試行
- 再試行時は既存本文と材料をさらに圧縮し、JSON形式を使わず完成本文だけを取得
- 本文のみ再試行で成功した場合、診断欄に `LM Studio文章生成（本文のみ再試行）が完了しました` と表示
- 取扱説明書に、文章作成の本文のみ再試行について追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 編集アクションボタンをアイコン化

- 通常編集画面の削除・キャンセル・保存ボタンを、ゴミ箱・X・チェックのアイコンボタンに変更
- タイルモード編集画面の削除・キャンセル・保存ボタンも同じアイコン表現に統一
- 各ボタンに `title` と `aria-label` を設定し、ホバー時の色で削除・保存の意味が分かるよう調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### URL・Tags折りたたみ表示の調整

- URL・Tags欄の開閉表示を `＋/－` から、PaletteやAI補助と同じ `▸/▾` 表記に変更
- 折りたたみ時の `未設定` 表示を、入力値と見分けやすい状態バッジ風に変更
- URLやTagsが設定済みの場合の概要表示も同じバッジ形式に統一

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカルブラウザで `▸/▾ URL・Tags` 表示、未設定バッジ、設定済み概要表示を確認

---

## 2026-05-14

### AI補助タブの初期表示を文章作成に変更

- AI補助パネルを開いた時、`文章作成` タブを初期選択するよう変更
- 読者メモ用のヘッダー生成ボタンは、読者メモタブ選択時のみ表示

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカルブラウザで `文章作成` タブが初期選択され、`読者メモ` の生成ボタンが非表示になることを確認

---

## 2026-05-14

### AI補助タブの並びを変更

**ファイル**: `output/ai-blog/ai-blog.html`

AI補助パネル内のタブボタン表示順を調整。

#### 変更内容

- `文章作成` タブを左、`読者メモ` タブを右に表示
- 初期表示を `文章作成` に変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 折りたたみ見出しの見た目を統一

**ファイル**: `output/ai-blog/ai-blog.html`

通常編集画面の折りたたみ見出しを、Palette欄と同じ控えめなテキスト型に統一。

#### 変更内容

- `URL・Tags` のトグルをボタン枠付きからPaletteと同じテキスト型に変更
- `AI補助` の折りたたみ見出し色をPaletteと同じ表示に調整
- 展開中だけ見出し色が強調されるよう統一

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 編集画面の表示をコンパクト化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

文章作成の長文結果と通常編集画面のURL・Tags欄で、編集画面が縦に伸びすぎないよう調整。

#### 変更内容

- 文章作成の生成結果欄に最大高さを設定し、長文は欄内スクロールで表示
- 通常編集画面のURL欄とTags欄を、まとめて開閉できる `＋ URL・Tags` トグルに変更
- URLまたはTagsがある記事では編集時に自動展開し、空の記事では折りたたみ表示に変更
- タイル編集オーバーレイ側のURL・Tags欄は従来通り維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 文章作成プロンプトを著者文体寄りに調整

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

文章作成タブのLM Studioプロンプトを、本ブログ著者の思考や文体に寄せた本文生成用に調整。

#### 変更内容

- 文章作成のプロンプトに、著者の書きぶり、入力の使い方、禁止事項を追加
- `論文形式` の表示を `論考形式` に変更
- `論考形式` では問題提起、補助線、再定義、比喩、結論または問いを自然に含む本文案を生成
- プロンプト説明文が `draft` に混入した場合の除外判定を追加
- 読者メモと保存後ポジティブコメントのプロンプトは変更なし

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 文章作成の生成量を少し拡大

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

速度短縮後の文章作成だけ、本文量を少し戻して生成内容を厚くするよう調整。

#### 変更内容

- 文章作成の既存本文コンテキストを少し増加
- 文章作成の `max_tokens` を少し増加
- 生成上限の目安を最大1100字程度に変更
- 読者メモと保存後コメントの軽量化は維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### AI生成全体をバランス型で軽量化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

読者メモ、文章作成、保存後コメントの生成時間を短縮するため、送信本文量と出力上限を調整。

#### 変更内容

- 読者メモ生成の送信本文量と初回 `max_tokens` を少し削減
- 文章作成の既存本文コンテキストと `max_tokens` を削減し、上限目安を最大900字程度に変更
- 保存後ポジティブコメントの本文送信量と `max_tokens` を削減
- 通常モデルの `EMPTY_RESPONSE` 再試行上限を抑え、Qwen系の大きめ再試行は維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 文章作成の本文判定を調整

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studioが有効な `draft` を返しているのに、文章案なしとして扱われるケースを修正。

#### 変更内容

- 文章作成用の本文判定を、読者メモ用の厳しいメタ文判定から独立
- `本稿では` など論文形式の自然な本文を弾かないよう調整
- JSON指示や出力形式説明の混入だけを除外する判定に変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 文章作成欄の未生成表示を省スペース化

**ファイル**: `output/ai-blog/ai-blog.html`

文章作成タブの未生成メッセージが欄の高さを広げていたため、表示をコンパクトに調整。

#### 変更内容

- 未生成時の文言を短く変更
- 未生成時だけフォントサイズ、行間、上余白を小さく調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### AI補助に文章作成タブを追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

通常編集画面のAI補助に、入力材料から本文案を生成する文章作成タブを追加。

#### 変更内容

- 通常編集画面のAI補助を `読者メモ` / `文章作成` のタブ構成に変更
- 文章作成タブに日記形式・論文形式の切替、材料入力欄、文章生成ボタン、生成結果欄を追加
- 生成文章は入力量に応じた長さにし、最大1200字程度に収めるプロンプトを追加
- **本文末尾へ追加** を押した場合だけ編集本文へ挿入し、自動挿入・自動保存はしない
- タイル編集画面は従来どおり読者メモのみを維持

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 保存後コメントを手動保存に変更

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studioのポジティブコメントを自動追記せず、ユーザーが保存ボタンを押した場合だけ本文へ保存するよう変更。

#### 変更内容

- ポジティブコメント生成後、自動で `comment:` 行を追記しないよう変更
- コメントカードに **コメントを保存** ボタンを追加
- ボタン押下時だけ本文末尾へ `comment: 生成コメント` を追記保存
- 保存済みの場合はカード上に保存済み表示を出し、同じ末尾行の重複追記を防止

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 保存後コメントを本文へ追記保存

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

保存後にLM Studioで生成したポジティブコメントを、表示だけでなく本文末尾へ保存するよう変更。

#### 変更内容

- LM Studioのポジティブコメント生成に成功した場合、本文末尾へ `comment: 生成コメント` を追記
- 既存本文がある場合は空行を挟み、本文が空の場合は `comment:` 行だけを保存
- LM Studio無効時や生成失敗時の保存完了文・失敗文は本文へ追記しない
- 同じ `comment:` 行が末尾にある場合は重複追記しない

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-14

### 推奨モデルに Gemma 4 を追記

**ファイル**: `output/ai-blog/ai-blog-取扱説明書.md`

LM Studio連携で `google/gemma-4-31b` と `google/gemma-4-e2b` による読者コメント生成が成功したため、AI Blog の推奨・動作確認済みモデルとして明記。

#### 変更内容

- 取扱説明書の LM Studio 手順に、推奨モデル `google/gemma-4-31b` を追記
- `google/gemma-4-e2b` でも読者コメント生成まで動作確認済みであることを追記
- 基本開発・動作確認は Gemma 4 系を中心に進める方針を記載
- Qwen 系などの推論系モデルは本文応答が空になる場合があるため、Gemma 4 系または通常のチャット向けモデルを使う注意書きを追加

#### 確認

- Markdown 文書のみの変更として差分を確認

---

## 2026-05-13

### Qwen系の `/no_think` 指示位置を調整

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

Qwen系モデルで `/no_think` を付与しても `reasoning_content` が出続け、`content` が空のまま `finish_reason=length` になるケースに追加対応。

#### 変更内容

- Qwen / QwQ / thinking 系モデルでは、system/userを分けず、1つの user メッセージ末尾に `/no_think` を置く形式へ変更
- Qwen系モデルで `EMPTY_RESPONSE` + `finish_reason=length` が続く場合、再試行時だけ `max_tokens` を大きめに増やして最終回答まで到達しやすくした
- 取扱説明書に、`/no_think` をユーザープロンプト末尾へ付けることを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### Qwen系モデルの思考出力を抑制

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

`qwen/qwen3.5-9b` が `reasoning_content` に思考過程だけを出し、`content` が空のまま `finish_reason=length` になるケースに対応。

#### 変更内容

- Qwen / QwQ / thinking 系モデルでは、LM Studio送信時に `/no_think` を付与
- system / user 両方で、内部推論・Thinking Process・手順説明を出力しないよう明示
- Qwen系モデルで `EMPTY_RESPONSE` + `finish_reason=length` になった場合、JSON再試行時に `max_tokens` を増やしすぎないよう変更
- 診断欄に `thinking: disabled (/no_think)` を表示
- 取扱説明書に Qwen 系モデルで `/no_think` を付与することを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### JSON生成の空応答再試行を調整

**ファイル**: `output/ai-blog/ai-blog.html`

推論系モデルが `reasoning_content` だけで短い `max_tokens` を使い切り、読者コメントが空応答になるケースに対応。

#### 変更内容

- JSON生成で `EMPTY_RESPONSE` かつ `finish_reason=length` の場合だけ、再試行時の `max_tokens` を増加
- 通常成功時の短い出力上限は維持
- 診断欄に再試行時の `max_tokens` を表示

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### AI補助欄を読者目線メモに一本化

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studio生成の待ち時間と失敗率を下げるため、AI補助の生成内容を1欄に整理。

#### 変更内容

- 改善提案・読者の疑問・次に書く一段落・リライト案の4欄を、読者目線のメモ1欄に統合
- LM Studioへの要求を `readerFeedback` だけに変更し、2〜4件の短いコメントを生成
- 続き段落・リライト案・項目別補完の追加生成処理を削除
- 診断表示を `読者コメント: n件` に変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### LM Studio生成時間を短縮

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studio連携の待ち時間を短くするため、不足項目の追加生成を最小限に変更。

#### 変更内容

- 改善提案・読者の疑問・リライト案が欠けた場合の個別追加生成を停止
- 「次に書く一段落」が欠けた場合だけ追加生成を試す構成に変更
- 欠けた項目は `0件` として診断に表示し、各欄は未生成プレースホルダーを維持
- 使わなくなったプレーンテキスト補完用の関数を削除

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### リライト案の未生成表示色を調整

**ファイル**: `output/ai-blog/ai-blog.html`

リライト案の未生成メッセージが生成済み本文と同じ色で表示される問題を修正。

#### 変更内容

- リライト案が未生成の時は、他欄のプレースホルダーと同じ薄い文字色で表示
- リライト案が生成済みの時だけ茶色系で表示
- ライトモードでも同じ表示ルールになるよう調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### リライト案の表示色を調整

**ファイル**: `output/ai-blog/ai-blog.html`

AI補助欄の表示を揃えるため、リライト案の文字色を他の生成項目と同系統に変更。

#### 変更内容

- リライト案の文字色を「次に書く一段落」と同じ茶色系に変更
- ライトモードでも同系統の色になるよう調整

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### リライト案の個別補完を追加

**ファイル**: `output/ai-blog/ai-blog.html`

最小生成でリライト案が未生成になるケースに対応。

#### 変更内容

- リライト案が欠けた場合、LM Studioへリライト専用のプレーンテキスト生成を追加
- リライト案も表示前に、要約・助言・プロンプト制約文ではないかを判定
- 通常生成後と最小生成後の両方で、不足している改善提案・読者の疑問・リライト案を補完するよう変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### 推論トークンで空応答になる場合の再試行を追加

**ファイル**: `output/ai-blog/ai-blog.html`

推論系モデルが `reasoning_content` だけで `max_tokens` を使い切り、`content` が空になるケースに対応。

#### 変更内容

- テキスト生成で `EMPTY_RESPONSE` かつ `finish_reason=length` の場合、`max_tokens` を増やして自動再試行
- 改善提案・読者の疑問・続き段落のプレーンテキスト生成の初期 `max_tokens` を増加
- 再試行後も失敗した場合、診断に初回詳細と再試行トークン数を残すよう変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### reasoning_content の表示混入を防止

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studio の推論系モデルが `content` を空にし、`reasoning_content` に思考過程や制約説明を返すケースに対応。

#### 変更内容

- `reasoning_content` / `reasoning` を通常の生成本文として採用しないよう変更
- 表示直前にも改善提案・読者の疑問・次に書く一段落の項目別フィルタを再適用
- `内容の核心`、`主要なテーマ`、`求められているのは`、途中で切れた疑問文などを除外
- 読者の疑問は疑問文として完結しているものを優先して表示

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### 制約文混入の除外と続き段落の表示色を調整

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studioがプロンプトの制約文や記事要約を各項目に返すケースに対応。

#### 変更内容

- `求められている`、`出力形式`、`続き本文だけ` などの制約説明を除外
- `記事の主要なテーマ` などの記事要約を改善提案・続き段落から除外
- `何を足す／削る／並べ替えるか` のようなプロンプト文の丸写しを改善提案から除外
- 「次に書く一段落」の文字色を改善提案・読者の疑問と同系統に変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### 続き段落の最小プロンプトフォールバックを追加

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studioがプレーンテキスト生成でも制約説明を返すケースに対応。

#### 変更内容

- プレーンテキスト生成がメタ説明になった場合、制約列挙をしない `本文` / `続き` 形式の最小プロンプトで再生成
- 最小プロンプトでも続き段落が取れない場合は `EMPTY_BARE_NEXT` として診断
- 診断に直前の応答抜粋と最小プロンプト応答抜粋を残すよう変更

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### LM Studio出力の項目別フィルタを強化

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studioが出力条件や記事要約を、読者の疑問・続き段落として返すケースに対応。

#### 変更内容

- 読者の疑問は疑問文らしい文だけを採用するよう変更
- 改善提案は、記事中の具体語や「足す・削る・並べ替える」などの編集意図がある文だけを採用
- `出力形式`、`JSON`、`番号・箇条書き` などプロンプト制約の混入を除外
- `記事の最後`、`論旨`、`強調されている` など、記事要約・論評の文を続き段落から除外
- 次に書く一段落のプレーン生成プロンプトに、要約・論評を禁止し、本文に合う具体語を含める指示を追加

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### LM Studio最小生成の不足項目補完を強化

**ファイル**: `output/ai-blog/ai-blog.html`

最小生成で「次に書く一段落」だけが出て、改善提案・読者の疑問が空になるケースに対応。

#### 変更内容

- 改善提案・読者の疑問が欠けた場合、LM Studioへプレーンテキストの個別生成を追加
- JSON生成が苦手なモデルでも、改善提案と読者の疑問を改行区切りで取得できるよう変更
- 「最後の一段落を示して終わっている」「指摘のみ」のような文を、続き段落として扱わない判定を追加

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### AI補助をLM Studio専用に整理

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

ブラウザ内JavaScriptによるローカル解析候補を削除し、AI補助欄をLM Studioの執筆支援に絞り込んだ。

#### 変更内容

- タイトル案・タグ案・要約・構成チェックの表示欄を削除
- 入力中の自動ローカル解析を停止し、AI補助生成は明示的な **生成** ボタンだけで実行
- LM Studio失敗時にローカル解析へフォールバックせず、診断と空状態を表示するよう変更
- 改善提案・読者の疑問・次に書く一段落・リライト案だけを表示
- 折りたたみ時のAI補助欄をPalette欄と同程度の高さに調整
- 折りたたみ時は **生成** / **設定** ボタンを非表示にし、展開時だけ表示
- 保存時コメントのローカル解析フォールバックを削除し、LM Studio無効・失敗時は通常の保存完了表示に変更
- 取扱説明書をLM Studio専用の説明に更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-13

### LM Studio続き段落の取得判定を強化

**ファイル**: `output/ai-blog/ai-blog.html`

LM Studio生成が成功扱いになるものの、「次に書く一段落」が表示されないケースに対応。

#### 変更内容

- LM Studio応答の `next_paragraph`、`次に書く一段落`、`続き段落` などの別キー名を `nextParagraph` として扱うよう変更
- タイトル案、タグ案、構成チェック、改善提案、読者の疑問、リライト案も別キー名を吸収する正規化処理を追加
- 続き段落が欠けたままの場合は成功扱いにせず、簡易生成・追加生成へ回すよう変更
- 補完生成プロンプトで `nextParagraph` を必須と明示
- `本文末尾に続ける60〜90字の完結文` のような形式説明の丸写しを無効扱いに変更
- 続き段落だけを再生成する追加フォールバックを追加
- 未生成時の案内文を薄い色で表示し、LM Studio生成結果と区別しやすく変更
- LM Studioが `案1`、`要約`、`続きの一段落` などの形式例をそのまま返さないよう、生成プロンプトから値の例示を削除
- 簡易応答がプレースホルダーだけの場合でも、続き段落専用生成を最後に試すよう変更
- JSON_PARSE が続く場合は、JSONを要求しないプレーンテキストの一段落生成へ切り替えるフォールバックを追加
- `ユーザーは...JSON...` のような作業説明文を生成結果として扱わない判定を追加
- `書くとよい`、`加えると`、`次の段落では` などの助言文を「次に書く一段落」として扱わない判定を追加
- LM Studio最小生成で改善提案・読者の疑問が空になる場合に備え、ローカル解析で補完する候補を追加
- `案1`、`タグ`、`要約` などのプレースホルダー値を表示候補から除外

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### 簡易生成の不足項目補完を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

簡易生成で読者の疑問や続き段落が欠けるケースに対応。

#### 変更内容

- 簡易生成・最小生成のJSON形式に `readerQuestions` を追加
- 簡易生成で `readerQuestions` または `nextParagraph` が欠けた場合、不足分だけを小さな追加生成で取得
- 追加生成でも、接続詞だけで始まる続き段落を避けるよう指示
- 取扱説明書に、不足分追加生成について追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### 短すぎるLM Studio出力の除外を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio が「ですが…」のような短すぎる続き段落だけを返すケースに対応。

#### 変更内容

- 要約・続き段落・リライト案・肯定コメントに最小文字数チェックを追加
- 「ですが」「しかし」「また」など接続詞だけの出力を無効扱いに変更
- 有効なAI補助項目がない場合は再試行・簡易生成・最小生成へ回すよう判定を強化
- プロンプトに、続き段落は接続詞だけで始めず、主語や具体語を含む完結文にする指示を追加
- 取扱説明書に短すぎる出力の扱いを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio生成が少ない場合の補完を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio生成は成功しているが、各項目が1件程度しか返らないケースに対応。

#### 変更内容

- タイトル案・タグ案・構成チェックで、LM Studio出力が少ない場合はローカル候補で不足分を補完
- 重複候補を除去して表示
- `SUCCESS` 診断に、補完込みの件数であることを表示
- 取扱説明書に、LM Studio出力とローカル候補の補完関係を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio 生成ログを表示

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

生成されないのにエラーも出ないケースを切り分けるため、成功・実行中の状態も診断欄に表示。

#### 変更内容

- 生成開始時に `RUNNING` として model、本文文字数、AI送信文字数、編集場所を表示
- 生成成功時に `SUCCESS` としてタイトル案・タグ案・改善提案・読者の疑問・続き段落の有無を表示
- 成功時も診断欄を残し、結果が空に見える場合の切り分けを容易化
- 取扱説明書に `RUNNING` / `SUCCESS` 表示について追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### 31B級モデル向けの最小生成フォールバックを追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

`google/gemma-4-31b` などの大きなローカルモデルで、簡易生成でもタイムアウトするケースに対応。

#### 変更内容

- LM Studio 生成を通常・簡易・最小の3段階に変更
- 通常生成の送信本文量と `max_tokens` を削減
- 簡易生成の `max_tokens` をさらに削減
- 簡易生成も失敗した場合、要約・改善提案・続き段落のみの最小生成へフォールバック
- JSON整形再試行時に `max_tokens` を増やさないよう変更
- 31B級モデルではタイムアウト上限を長めに設定
- 取扱説明書に `LM Studio生成（最小）` と大きなモデルでの注意を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio 失敗時の診断表示を追加

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studioから取得できていないのに、エラーコードが見えない問題に対応。

#### 変更内容

- AI補助パネルに LM Studio 診断表示欄を追加
- HTTPエラーは `HTTP_400` のような診断コードとして表示
- HTTPコードがない失敗は `TIMEOUT`、`EMPTY_RESPONSE`、`JSON_PARSE`、`FETCH_ERROR` などの診断コードとして表示
- 診断欄に model、max_tokens、経過時間、finish_reason、応答構造、応答抜粋を表示
- AI補助が折りたたまれていても分かるよう、AI補助バーとトーストにも `[TIMEOUT]` などの診断コードを表示
- LM Studio設定がオフの場合は `[DISABLED]`、応答はあるがAI補助項目がない場合は `[EMPTY_ASSIST]` として表示
- 生成中はボタンを `生成中` に変更し、二重実行を防止
- ローカル解析へ戻った場合は古い診断表示をクリア
- 取扱説明書に診断コード表示について追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio 生成依頼を軽量化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

ローカルモデルで長文執筆支援がタイムアウトしやすい問題に対応。

#### 変更内容

- LM Studio へ送る本文が長い場合、先頭と末尾を中心に圧縮して送信
- AI補助生成の出力量を短めに調整し、1回の生成を軽量化
- 再試行時の `max_tokens` が大きくなりすぎないよう制限
- 簡易生成の `max_tokens` を削減
- `/v1/chat/completions` に `stream: false` を明示
- 取扱説明書に、長文はAI送信時に圧縮されることを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio 生成結果のJSON解析を安定化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio には接続できているが、生成結果がJSONとして解釈できずローカル解析へ切り替わるケースに対応。

#### 変更内容

- LM Studio 応答から複数のJSON候補を抽出し、解析できる候補を採用するよう変更
- JSON文字列内に混ざった生の改行・タブや、末尾カンマを修復してから再解析する処理を追加
- JSON解析に失敗した場合、JSONのみを返すよう自動再試行
- `message.content` が空の場合、reasoning系フィールドや `choices[].text` も確認し、空応答も自動再試行・短縮版生成の対象に変更
- 長い執筆支援がJSON解析失敗またはタイムアウトした場合、短縮版のLM Studio生成へ自動フォールバック
- AI補助バーの失敗表示に、失敗理由の先頭を表示
- 取扱説明書に、再試行と `LM Studio生成（簡易）` への切り替えについて追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio 生成タイムアウトと接続テストを改善

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

接続確認は成功するがAI補助生成で失敗するケースに対応。

#### 変更内容

- LM Studio の生成APIタイムアウトを、通常60秒・長文支援120秒へ延長
- 接続テストを `/v1/models` の確認だけでなく、短い `/v1/chat/completions` 生成まで確認する方式に変更
- `/v1/chat/completions` がHTTPエラーを返した場合、LM Studio側のエラー本文をトーストに表示
- タイムアウト時に原因が分かるメッセージを表示
- 取扱説明書に、接続テストが短い生成まで確認することと、長文生成は最大2分待つことを追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio 執筆支援を強化

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio 接続時の AI補助を、軽い解析からブログ執筆支援へ拡張。

#### 変更内容

- LM Studio へのプロンプトを強化し、本文中の具体語を拾った改善提案・読者の疑問・次に書く一段落・リライト案を生成
- AI補助パネルに `改善提案`、`読者の疑問`、`次に書く一段落`、`リライト案` 欄を追加
- `次に書く一段落` を本文末尾へ追加するボタンを追加
- 生成トークン上限を増やし、より長い執筆支援結果を受け取れるよう調整
- ローカル解析時は従来の軽量候補を維持し、LM Studio 生成時のみ執筆支援欄を活用

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### LM Studio API Token 認証に対応

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

LM Studio 側で API token 認証を有効にしている場合でも接続できるよう、設定モーダルに API Token 欄を追加。

#### 変更内容

- LM Studio 設定に API Token 欄を追加
- `/v1/models` と `/v1/chat/completions` のリクエストで、入力済み token を `Authorization: Bearer ...` として送信
- API Token 設定を `localStorage` の `aiblog_lmstudio_api_token` に保存
- 取扱説明書に API token 認証時の入力手順と保存場所の注意を追記

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### AI補助タイトルを Palette 同様の開閉表示に変更

**ファイル**: `output/ai-blog/ai-blog.html`、`output/ai-blog/ai-blog-取扱説明書.md`

Palette 欄と同様に、AI補助欄も左側の三角付きタイトルをクリックして展開 / 折りたたみできるように変更。

#### 変更内容

- 通常編集画面とタイル編集画面の `AI補助` タイトルにクリックイベントを追加
- 右端の開閉ボタンを削除
- 開閉状態に応じてタイトルを `▸ AI補助` / `▾ AI補助` に切り替え
- キーボード操作用に `Enter` / `Space` でも開閉できるよう対応
- タイトル部分に `cursor: pointer` と `role="button"` を追加
- 取扱説明書の操作説明を更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施

---

## 2026-05-12

### AI補助付き My Blog 派生アプリを新規作成

**ファイル**: `output/ai-blog/ai-blog.html`

My Blog をベースに、別アプリとして AI Blog を作成。

#### 基本方針

- 既存 `output/myblog/myblog.html` は変更しない
- My Blog の JSON データ形式と互換
- `localStorage` キーは `aiblog_*` に分離
- OpenAI API 等は使わず、ブラウザ内のローカル解析で補助機能を提供

#### 追加機能

- 編集画面に **AI補助** パネルを追加
  - タイトル案
  - タグ案
  - 要約
  - 構成チェック
- タイトル案クリックで Title 欄へ反映
- タグ案クリックで Tags 欄へ追加
- 入力中の自動再解析と手動更新ボタンを実装
- 記事保存時に、文章の特徴に応じたポジティブコメントを表示
- チャットモード投稿時にも同じポジティブコメントを表示

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカルブラウザで折りたたみ初期表示、展開、候補更新、LM Studio設定モーダルの開閉を確認
- ローカルブラウザで LM Studio 未起動時のAI補助生成フォールバックと保存時コメントフォールバックを確認

---

## 2026-05-12

### AI補助欄の省スペース化

編集画面で本文入力欄を広く使えるよう、AI補助欄の表示を整理。

#### 変更内容

- AI補助欄を通常は1行バーのみ表示する折りたたみ形式に変更
- 展開時のみタイトル案・タグ案・要約・構成チェックを表示
- 開閉状態を `aiblog_ai_assist_collapsed` に保存し、通常編集画面とタイル編集画面で同期
- LM Studio の Base URL / Model / ON-OFF / 接続テストを専用設定モーダルへ移動
- 入力中のローカル解析は維持し、折りたたみ中でも候補は更新

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
- ローカルブラウザで新規作成、AI補助候補生成、保存時コメント、チャット投稿時コメントを確認

---

## 2026-05-12

### LM Studio 連携によるAI補助強化

AI補助パネルに、任意で LM Studio のローカルAPIを使う設定を追加。

#### 追加機能

- Base URL / Model / LM Studio ON-OFF / 接続テストの最小設定UIを追加
- 既定 Base URL は `http://localhost:1234/v1`
- Model 欄が空の場合は `/v1/models` の先頭モデルを使用
- AI補助の **生成** ボタンで `/v1/chat/completions` にリクエストし、タイトル案・タグ案・要約・構成チェックをJSONで取得
- 記事保存時・チャット投稿時のポジティブコメントも LM Studio 生成に対応
- LM Studio 未起動、モデル未ロード、JSON解析失敗、CORS等の接続失敗時は従来のローカル解析へ自動フォールバック

#### データ互換

- LM Studio 設定は `localStorage` の `aiblog_lmstudio_*` に保存
- 記事JSONには LM Studio 設定・AI生成結果・コメント履歴を保存しない

#### 確認

- `node --check` で `ai-blog.html` の埋め込み JavaScript 構文チェックを実施
